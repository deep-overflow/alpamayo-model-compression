# scope-matched dual — 폐루프 열세의 원인이 레이어 범위인가 (2026-08-22)

## 배경

폐루프 150씬에서 우리 dual과 동료의 LLM-Pruner arm은 **종합 점수가 구분 불가**다
(dual−lp_r50 +0.018 [−0.029,+0.066] ns, dual−lp_r50_dual +0.012 ns). 그런데 세부 지표는
계통적으로 갈린다:

| | score | col@f / any | offroad | d2gt |
|---|---|---|---|---|
| baseline | 0.750 | 0.040 / 0.133 | 0.070 | 2.89 |
| dual (36층 균일, 24.0%) | 0.828 | **0.023 / 0.067** | 0.067 | **3.32** |
| lp_r50 (30층 4–34, 25.0%) | 0.810 | 0.057 / 0.160 | **0.037** | **2.88** |

즉 **우리는 충돌이 절반, LP는 경로 정밀도(d2gt)와 오프로드가 우수**하다. 두 방법은
기준·목적함수·절단 축이 사실상 같고(둘 다 dual = max(rank I_traj, rank I_CoC), k/v 동결),
가장 큰 구조적 차이는 **레이어 범위**다: LP는 업스트림 `--block_*_layer_start/end` 관례대로
layers 4–34만 자르고 앞 4층·뒤 2층을 온전히 남긴다. 우리는 36층 균일이다.

**H-S**: d2gt/offroad 열세는 기준이 아니라 **레이어 범위** 때문이다. 앞뒤 층(저수준 공간·
차선 표현)을 보존하면 d2gt가 baseline 수준(≈2.9)으로 내려가고, 대신 중간층을 더 깊게
잘라야 하므로 충돌 이점 일부를 잃는다.

## 설계 (one-factor: 범위만 변경)

`dualscope_u40` — dual_u40_v2와 **기준·calibration·축·예산이 모두 동일**하고 절단 위치만
다르다.

- 기준: `max(rank I_traj, rank I_CoC)` (importance_v2, calib_100) — dual과 동일.
- 축: VLM Q head / MLP 채널만, expert·KV 불변 — dual과 동일.
- 범위: **layers 4–34 (30층)**, 0–3·34–35는 손대지 않음 (LP 관례와 동일).
- 예산: **정확히 −2,657,452,032** (dual_u40_v2와 파라미터 단위까지 일치). 30층에 같은
  예산을 넣으려면 층당 절단이 깊어진다 — 깊이 스케일 비율 r×36/30 = 0.4783에서
  head는 반올림 15/32, MLP 채널 수는 총량이 목표에 정확히 떨어지도록 역산:

      30층 × 15 head × 1,048,576 = 471,859,200
      나머지 2,185,592,832 / 12,288 = 177,864 채널 = 24층 × 5929 + 6층 × 5928

  잔여 1채널은 scope 앞쪽 24개 층에 하나씩 분배(결정적). 총합 = 2,657,452,032 ✓
- 선택 규칙은 다른 모든 arm과 같은 `mask_lib.select_mask_ratios`(레이어 내 최저 점수 절단).

## 사전 등록 게이트

- **S0 (무결성)**: 제거 파라미터 == 2,657,452,032 정확 일치, layers 0–3·34–35의 kept-set이
  전부 온전(32 head / 12288 채널), scope 층은 17 head / 6359–6360 채널 보존.
- **S1 (개루프, test_500)**: paired ΔminADE@6 vs dual. 방향 예측 없음(양쪽 다 정보) —
  범위를 좁히면 남은 층이 더 깊게 잘리므로 개루프는 나빠질 수 있다.
- **S2 (주 판정, 폐루프 150씬)**: d2gt와 offroad가 lp_r50 수준(≈2.9 / ≈0.04)으로 내려가는가.
  - 내려가면 → H-S 확인: "앞뒤 층 보존 = 경로 정밀도" (그리고 충돌이 오르면 트레이드오프 확정).
  - 안 내려가면 → 범위 가설 기각, d2gt 차이는 기준·정책 차이에서 온다.
  - **주의**: 폐루프 종합 점수는 n=150에서 해상도 0.067이라 score로는 판정하지 않는다.
    d2gt(연속값)와 offroad는 씬-페어드로 훨씬 민감하므로 이 둘로 판정한다.
- **S3 (부기록)**: 충돌(과실/전체), 진행도, wrong_lane, CoC degen, LingoQA.

## 구현

| 파일 | 내용 |
|---|---|
| `make_slim.py` | `dualscope_u40` 분기 + `scope_matched_counts()` (예산 역산), `--scope-start/--scope-end` |
| `paper_numbers.py` | ARMS에 `dualscope` 3셋 등록 |

빌드는 마스크 방식이라 `--no-state` 가능(가중치 재작성 없음).

## 실행 (미착수 — 사용자 지시로 코드 작성까지만)

1. 빌드 + S0 검증 — ~40분, Ada 6/7
2. 개루프 3셋 — ~3h
3. 폐루프 150씬 (S2 주 판정) — ~8h, shards 경로
4. LingoQA — ~40분

## 결과 — S0 통과, S1 열세 (2026-08-22, rollout-importance-dual 세션 실행)

원 세션이 코드까지 올리고 종료해, 사용자 지시로 다른 세션이 빌드·평가를 실행했다
(exp-id·코드는 그대로 사용). 카드 4·5, 고정 프로토콜(rollout-only, K=8 → @6, Ada, paired seed).

**S0 (무결성) PASS.** 빌드 전 CPU 대조로 확인: 제거 파라미터 2,657,452,032 정확 일치,
layers 0–3·34–35 무손상, scope 30층은 15 head + 5928(+1×24) 채널 절단. 재현한 dual 마스크가
shipped `slim_dual_u40_v2`의 slim_meta와 완전 일치. 두 arm의 차이:

| | 공통 제거 | dual만 | dualscope만 | Jaccard | kept-overlap (vs dual) |
|---|---|---|---|---|---|
| Q head | 390 | 78 | 60 | 0.739 | 0.9123 |
| MLP 채널 | 146,940 | 29,388 | 30,924 | 0.709 | 0.8838 |

제거 파라미터의 **16.7%(442,908,672)**가 다른 유닛에 떨어진다. 단, kept-overlap 척도로 보면
캘리브레이션 잡음 바닥(calib 100클립 50:50 분할: Q 0.8596 / MLP 0.7818)보다 이동폭이 **작다**.

**S1 (개루프) — dualscope가 유의하게 열세.** minADE@6 / minFDE@6 평균:

| set | baseline | dual | dualscope | Δ vs dual (med [CI]) | Δ vs baseline |
|---|---|---|---|---|---|
| val 500 | 0.824 / 2.205 | 0.890 / 2.330 | 0.975 / 2.551 | **+0.0236 [+0.0083, +0.0459]\*** (FDE +0.0742\*) | +0.1074\* |
| test 500 | 0.842 / 2.267 | 0.950 / 2.546 | 1.026 / 2.765 | **+0.0389 [+0.0242, +0.0591]\*** (FDE +0.1214\*) | +0.1052\* |
| OOD-val 262 | 1.000 / 2.496 | 1.119 / 2.851 | 1.183 / 3.063 | +0.0379 [−0.0036, +0.0849] (FDE +0.0225) | +0.1130\* |

CoC degen: dualscope 4.2 / 3.2 / 6.1% vs dual 1.4 / 3.0 / 3.4% — val에서 3배.

읽기: 두 in-dist 셋에서 유의차가 나왔으므로 "개루프로는 판별 불가"가 아니라 **LP식 스코프가
개루프에서 열세**라는 정보가 있다(baseline 대비 손실이 dual의 1.8–2배). 남은 층을 더 깊게
자르는 대가가 앞뒤 층 보존의 이득보다 크다는 뜻이고, degen 상승은 그 대가가 추론 채널에도
간다는 부수 증거다.

**S2는 여전히 미검증.** 개루프 열세와 폐루프 경로 정밀도(d2gt 3.32 vs lp_r50 2.88, offroad
0.067 vs 0.037)는 다른 축이다. lp_r50의 낮은 d2gt가 스코프 때문인지 OSSCAR 재구성·다른 요인
때문인지는 폐루프 150씬을 돌려야 갈린다 — S2 설계는 그대로 유효하다.

산출물: `outputs/dualscope_u40_{indist,test,oodval}`, 체크포인트 `outputs/slim_dualscope_u40`
(마스크 전용, `--no-state`). 재계산은 `paper_numbers.py`의 `dualscope` 키.

## 상태

2026-08-22 사용자 승인. 코드 작성 완료(fd2a032), **S0·S1 완료 — S2(폐루프)·LingoQA 미실행.**
